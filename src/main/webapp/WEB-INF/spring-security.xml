<beans:beans xmlns="http://www.springframework.org/schema/security"
             xmlns:beans="http://www.springframework.org/schema/beans"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="http://www.springframework.org/schema/beans
	http://www.springframework.org/schema/beans/spring-beans-4.2.xsd
	http://www.springframework.org/schema/security
	http://www.springframework.org/schema/security/spring-security-4.2.xsd">

    <http auto-config="true" use-expressions="true">
        <intercept-url pattern="/login**" access="hasRole('ROLE_ANONYMOUS')" />
        <intercept-url pattern="/**" access="hasRole('ROLE_ADMIN')" />

        <!-- access denied page
        <access-denied-handler error-page="/403" /> -->

        <form-login
                login-page="/login"
                login-processing-url="/login"
                default-target-url="/main"
                authentication-failure-url="/login2?error"
                username-parameter="login"
                password-parameter="password" />
        <logout logout-success-url="/login?logout"  />
        <!-- enable csrf protection -->
        <csrf disabled="true"/>
    </http>

    <authentication-manager>
        <!--<authentication-provider>
            <jdbc-user-service data-source-ref="dataSource"
               users-by-username-query=
                       "select user_mail as email, user_password as password, user_is_blocked as anabled from users where user_mail=?"
               authorities-by-username-query=
                       "select user_mail as email, role_name as role from users u left join roles r on r.role_id = u.user_role_id where u.user_mail =?  " />
        </authentication-provider>-->
        <authentication-provider>
            <user-service>
                <user name="ray@mail.ru" password="12345678" authorities="ROLE_ADMIN,ROLE_USER"/>
                <user name="user1" password="1111" authorities="ROLE_USER"/>
                <user name="user2" password="2222" disabled="true" authorities="ROLE_USER"/>
            </user-service>
        </authentication-provider>
    </authentication-manager>

</beans:beans>